name: platform-engineering
services:
    hello-world-web:
        annotations:
            compose.score.dev/workload-name: hello-world
        build:
            context: ./workloads/hello-world/app
        hostname: hello-world
        ports:
            - target: 8080
              published: "8080"
