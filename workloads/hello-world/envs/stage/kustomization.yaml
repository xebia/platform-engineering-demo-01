apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Promotion knob: the image tag this env runs. Bump via 'make release'/'make promote'.
resources:
  - ../../dist/k8s
images:
  - name: localhost:5001/hello-world-app
    newTag: "0.1.2"
